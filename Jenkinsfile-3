pipeline {
    agent any
    tools {
        maven 'maven'
        ansible 'ansible' // Ensure Ansible plugin is configured correctly in Jenkins
    }
    environment {
        DOCKERHUB_CREDENTIALS = credentials('jacek-docker')
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm // Or your Git configuration
            }
        }
        stage('Compile') {
            steps {
                sh 'mvn compile'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'mvn package'
            }
        }
        stage('Run Ansible Playbook') {
            steps {
                ansiblePlaybook(
                    playbook: 'docker_deploy2.yml', // Path relative to workspace root
                    extras: "-e BUILD_NUMBER=${BUILD_NUMBER}"
                )
            }
        }
    }
    post {
        always {
            sh 'docker logout'
        }
    }
}
