pipeline {
    agent any
    tools {
        maven 'maven'
    }
    stages {
        stage('Compile') {
            steps {
                sh 'mvn compile'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'mvn package'
            }
        }
        stage('Build and Deploy') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'jacek-docker', usernameVariable: 'DOCKER_USERNAME', passwordVariable: 'DOCKER_PASSWORD')]) {
                    sh '''
    echo "Docker Username: $DOCKER_USERNAME" // Verify (Masked in Jenkins console)
    echo "Docker Password: $DOCKER_PASSWORD" // Verify (Masked in Jenkins console)

    ansible-playbook docker_deploy2.yml -e "docker_username=$DOCKER_USERNAME docker_password=$DOCKER_PASSWORD build_number=$BUILD_NUMBER"
'''
                }
            }
        }
    }
    post {
        always {
            sh 'docker logout'
        }
    }
}
